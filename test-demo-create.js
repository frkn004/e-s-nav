const { PrismaClient } = require('@prisma/client'); const bcrypt = require('bcryptjs'); const prisma = new PrismaClient(); async function testDemoCreate() { try { const hashedPassword = await bcrypt.hash('123456', 12); const firma = await prisma.firmalar.create({ data: { ad: 'Test Demo Firma', email: 'test@demo.com', telefon: '5551234567', lisans_baslangic: new Date(), lisans_bitis: new Date(Date.now() + 24*60*60*1000), aktif: true, paket_tipi: 'TEMEL', guncelleme_tarihi: new Date() } }); console.log('Firma oluşturuldu:', firma.id); } catch (e) { console.error('Hata:', e.message); } finally { await prisma.$disconnect(); } } testDemoCreate();
